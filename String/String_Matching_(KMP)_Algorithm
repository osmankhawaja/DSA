#include<bits/stdc++.h>
using namespace std;
#define el "\n"
#define int long long
#define all(n) n.begin(),n.end()
#define rll(n) n.rbegin(),n.rend()
#define vi vector<int>
#define pb push_back
#define forc(i, n) for(int i=0; i<n; i++)
#define ff first
#define ss second
const int M=1e9+7;
const int inf=1e18;

///complexity(n+m) n-->length of text, m-->length of pattern

vector<int>create_lps(string &pattern)
{
    int n=pattern.size();
    vector<int>lps(n);
    lps[0]=0;
    int i=0, j=1;
    while(j < n)
    {
        if(pattern[i] == pattern[j])
        {
            lps[j]=i+1;
            i++; j++;
        }
        else 
        {
            if(i != 0)i=lps[i-1];
            else 
            {
                lps[j]=0;
                j++;
            }
        }
    }
    return lps;
}

void kmp(string &text, string &pattern)
{
    vector<int>lps=create_lps(pattern);

    int i=0, j=0, cnt=0;
    while(i < text.size())
    {
        if(text[i] == pattern[j])
        {
            i++; j++;
            if(j == pattern.size())
            {
                cnt++;
                j=lps[j-1];
            }
        }
        else 
        {
            if(j != 0)j=lps[j-1];
            else i++;
        }
    }
    cout<<cnt<<el;
}


void solve()
{
    string text, pattern;
    cin>>text>>pattern;

    kmp(text, pattern);
}

int32_t main()
{
    ios_base::sync_with_stdio(0); cin.tie(0); cout.tie(0);
    int t=1;
    // cin>>t;
    while(t--)
     solve();
}
